package com.bitbenders.theentity.di

/**
 * Configuration for mock vs. real backend.
 * Set USE_MOCK to true for frontend development without a real backend.
 */
object MockConfig {
    // Toggle this to switch between mock and real implementations
    const val USE_MOCK = true
}

